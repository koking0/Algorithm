#include<iostream>
using namespace std;
int main()
{
	int num[] = {1,2,3,4,5,6,7,8,9};
	int a = 0 , b = 0, c = 0, d = 0, e = 0;
	int i = 0, j = 0, k = 0, h = 0, g = 0;
	int n = 0;
	while(i<9)
	{ 
		a = num[i];
		i++;
		while(j < 9)
		{
			if(num[j] == a)
				j++;
			b = num[j];
			j++;
			while(k < 9)
			{
				while(num[k] == a || num[k] == b)
				{
					k++;
				}
				c = num[k];
				k++;
				while(h < 9)
				{
					while(num[h] == a || num[h] == b || num[h] == c)
					{
						h++;
					}
					d = num[h];
					h++;
					while(g < 9)
					{
						while(num[g] == a || num[g] == b || num[g] == c || num[g] == d)
						{
							g++;
						}
						e = num[g];
						g++;
						if((a*10+b)*(c*100+d*10+e) == (a*100+d*10+b)*(c*10+e))
							n = n + 1;

					}
				}
			}
		}
	}

	cout<<"计算结果总数为："<<n<<endl;
	cout<<num[3]<<endl;


return 0;





}